// Re-export the base fixture from the package
// Override or extend test/expect here if needed
// Re-export the standard Playwright test/expect fixture
export { test, expect } from "@playwright/test";
